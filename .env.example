BACKEND_URL=http://127.0.0.1:8080
BACKEND_WS_URL=ws://127.0.0.1:8080/v1/events
APP_ORIGIN=http://127.0.0.1:3000
REALTIME_CALL_URL=https://api.openai.com/v1/realtime/calls
SESSION_COOKIE_SECURE=false
# Enable only when the backend explicitly permits public demo sessions.
DEMO_LOGIN_ENABLED=false
KIVOF_SURFACE=product
PORT=3000
HOSTNAME=0.0.0.0
WEB_IMAGE_TAG=local

# External frames are limited to the presentation route.
DECK_FRAME_ORIGINS=https://player.vimeo.com,https://adiy.ch
